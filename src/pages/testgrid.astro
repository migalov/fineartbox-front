<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test Grid</title>
    <style>
        body {
            margin: 0;
        }
        .fab-template {
            display: grid;
            /* grid-template-columns: [padding-breakout-start] 2rem [content-page-start] 12fr [content-page-end] auto [header-start] 2fr [padding-breakout-end];
            m */
            background-color: #6f96ff46;
            grid-template-columns: 6fr 1fr;
            max-width: 1440px;
            gap: clamp(2rem, 1.6479rem + 1.5023vw, 3rem);
            height: 100vh;
            margin: 0 auto;
            /* 
             */
        }
        .fab-test-header {
            background-color: aquamarine;
        }

        .fab-test-page {
            background-color: rgb(34, 148, 255);
        }
    </style>
</head>
<body>
    <div class="fab-template">
        <div class="fab-test-header">

        </div>
        <div class="fab-test-page">

        </div>

    </div>
</body>
</html>